//
   Created by anton on 5/14/2015.

doctype
html
   head
      title Template
      link(rel="stylesheet",href="css/main.css")
   body
      section
         .controls
            .newGameCtrl
               input(type="checkbox" id="check")
               label.newMain(for="check")
                  .newMainText new
               .newContainer
                  .newPlate
                     .boardCellCircle(id="new-O")
                  .newPlate
                     .boardCellCross(id="new-X")
            .sizeCtrl
               .newMain
                  .newMainText(id="scale-Up") +
            .sizeCtrl
               .newMain
                  .newMainText(id="scale-Down") -
            .messages
               .messagesContainer
                  .newMainText(id="message") try to get 5 in a row!
         .board
            - for (var x = 0; x < 15; x++)
               .boardRow
                  - for (var y = 0; y < 15; y++)
                     .boardCol
                        .boardCell(id=x + "-" + y)
      script(src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.2/jquery.min.js")
      script(src='https://www.gstatic.com/firebasejs/4.2.0/firebase.js')
      script.
        // Initialize Firebase
        var config = {
        apiKey: "AIzaSyCYOlqxAZk19t23LXHIff5F_E79_dtnYuY",
        authDomain: "gomoku-401d0.firebaseapp.com",
        databaseURL: "https://gomoku-401d0.firebaseio.com",
        projectId: "gomoku-401d0",
        storageBucket: "gomoku-401d0.appspot.com",
        messagingSenderId: "786240642237"
        };
        firebase.initializeApp(config);
      script(src="js/bundle.js")
